package org.project;

public class Main {
    public static void main(String[] args) {
        String programVersion = "0.1";

        System.out.println(String.format("CSV Relational Database Management System v%s", programVersion));

        // load the current state of the specified csv file into memory since it may have changed since last read

        // load the current state of the database

        // load the database object
        // Note: If a database object already exists, a pointer to the current object will be returned

        // add the new rows to the database

        // print the top 10 rows from the database to the console
    }
}
